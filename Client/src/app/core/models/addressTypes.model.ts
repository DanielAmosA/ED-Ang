import { BaseType } from './baseType.model';

// The interface responsible for the structure declaration of the address types entity
export interface AddressTypes extends BaseType {
  addressTypeName : string
}
