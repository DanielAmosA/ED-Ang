import { BaseType } from "./baseType.model";

// The interface responsible for the structure declaration of the customer types entity
export interface CustomerTypes extends BaseType {
  customerTypeName : string
}
