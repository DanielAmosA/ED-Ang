// The type responsible for the description of the float animation mode
export type TFloatMode = 'start' | 'end';

// The type responsible for the description of the rotate animation mode
export type TRotateMode = 'normal' | 'rotated';

// The type responsible for the description of button mode
export type TButtonMode = 'normal' | 'hover';
