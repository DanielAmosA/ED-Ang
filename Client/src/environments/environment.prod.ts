// Setting the environment (Production)
// These settings are used to configure the API connection details.
export const environment = {
  production: true,
  apiConfig: {
    apiUrl: 'https://api.production.com/api',
    timeout: 10000
  }
};
