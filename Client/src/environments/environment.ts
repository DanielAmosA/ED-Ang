// Setting the environment (Development)
// These settings are used to configure the API connection details.
export const environment = {
  production: false,
  apiConfig: {
    apiUrl: 'http://localhost:5000/api',
    timeout: 30000
  }
};
